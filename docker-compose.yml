services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: wibe-landing:latest
    container_name: wibe-landing
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    restart: unless-stopped
